{
  "name": "@neighborhood/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Local Neighborhood Social Network",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
